define(function(require){

    /**
     * Put all run-once / bootstrap / config calls here.
     * Place all calls to scripts that only need to run once and not return an object here.
     */
    require('util/hotkeys');


    require('admin/test');

});
